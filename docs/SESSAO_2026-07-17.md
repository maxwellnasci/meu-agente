# Sessão 2026-07-17 — Correção do deploy falho do fix do Bug 4 (SQLite busy_timeout)

## Contexto

Sessão anterior (`docs/SESSAO_2026-07-16.md`, retomada final) tinha deixado o
Bug 4 como "candidato mais forte até agora, causa raiz ainda não fechada":
mecanismo de bloqueio composto do `busy_timeout` síncrono do `node:sqlite`
confirmado via reprodução controlada (`bug4-sqlite-compound-repro.test.ts`),
mas sem identificar quem segurava o lock no incidente real de 15/07.

O Antigravity reportou ter capturado o travamento **ao vivo** (não mais
simulado) durante uma mensagem real do WhatsApp, aplicado a correção
(`busy_timeout` 30s → 3s em 3 arquivos) e reiniciado produção com ela ativa.
Pedido explícito do usuário: auditar tudo com rigor antes de confiar —
mesma disciplina das retomadas anteriores.

## Auditoria (antes desta sessão de correção)

Resumo dos achados da auditoria completa feita a pedido do usuário:

1. **A "captura ao vivo" não prova o que foi alegado.** Os 2 arquivos
   `report.*.json` existentes (`/home/max/.openclaw/logs/`) datam de 20s
   depois do boot do container — não do incidente real — e têm
   `javascriptStack` completamente vazio. Um experimento controlado (script
   Node travando de propósito em loop síncrono + `SIGUSR2` no meio) provou
   que `--report-on-signal` **não consegue** capturar a pilha durante um
   bloqueio síncrono real: o relatório só é escrito depois que o bloqueio
   termina, quando o processo já está ocioso de novo. Os 3 sinais `SIGUSR2`
   reais enviados durante o incidente verdadeiro (21:33–21:36 do dia 16/07,
   `heartbeat-incidents.log`) não geraram nenhum relatório.
2. **O que É real e ao vivo:** nos logs brutos do gateway (não em nenhum
   doc), o incidente real de 21:33–21:36 do dia 16/07 mostra "WhatsApp Cloud
   reply started" repetindo a cada 6s sem completar, e a linha decisiva
   `[diagnostic] liveness warning: eventLoopDelayMaxMs=30064.8` — evidência
   genuína de ~30s de event loop congelado, batendo quase exato com o
   `busy_timeout` antigo. O campo `work=` da mesma linha aponta para
   `model_call:stream_progress`, não explicitamente para uma escrita SQLite
   — corrobora a família do bug (II: processo fisicamente travado), mas não
   fecha sozinho a causa específica.
3. **A correção do código está tecnicamente correta** (diff nos 3 arquivos
   conferido linha a linha, mais 2 call sites que herdam a mudança via
   constante compartilhada), e o teste de reprodução controlado, rodado de
   novo com o novo valor, mostrou o comportamento mudando exatamente como
   esperado (bloqueios de ~3s em vez de ~30s).
4. **2 testes pré-existentes quebraram** com a mudança
   (`openclaw-state-db.test.ts`, `openclaw-agent-db.test.ts`, ambos
   `expect(busy_timeout).toBe(30_000)` desatualizado) — mesmo padrão da
   tentativa do DeepSeek que quebrou 4 testes antes.
5. **Achado decisivo: a correção nunca chegou em produção.** Grep direto no
   bundle compilado dentro do container rodando mostrou `busy_timeout =
   30000` ainda presente em `apply-*.js`, `backup-create-*.js` e
   `openclaw-state-db-*.js`. A imagem `openclaw:local` rodando tinha sido
   criada às 18:37 do dia 16/07 — quase 3h30 **antes** do commit do fix
   (22:04:57 do dia 16/07). O restart de produção feito nessa madrugada
   (05:37 do dia 17/07) só reiniciou o processo com a **mesma imagem
   antiga** (`docker compose restart`/`up` sem rebuild não troca a imagem).
   Produção estava rodando, ao vivo, com o `busy_timeout` de 30s original —
   exatamente a condição do bug — sem que ninguém tivesse percebido.

Veredito da auditoria: Bug 4 não estava resolvido, e o passo mais urgente
era corrigir o processo de deploy antes de qualquer outra coisa.

## Correção aplicada nesta sessão

Ordem pedida explicitamente pelo usuário, executada e verificada passo a
passo:

### 1. Testes desatualizados corrigidos

`openclaw/src/state/openclaw-state-db.test.ts:584` e
`openclaw/src/state/openclaw-agent-db.test.ts:329` — `expect(...).toBe(30_000)`
→ `expect(...).toBe(3_000)`, para refletir o comportamento intencional do
fix `42af0a959a`, não um bug. Commitado em `openclaw/` (HEAD destacado, em
cima do commit do fix): `0e70b5f371`.

### 2. Suíte completa re-rodada

```
src/state/openclaw-state-db.test.ts (18 testes)
src/state/openclaw-state-db.permissions.test.ts
src/state/openclaw-agent-db.test.ts (13 testes)
src/infra/backup-create.test.ts (38 testes)
src/agents/auth-profiles.sqlite-store.test.ts (8 testes)
src/plugin-state/plugin-state-store.test.ts
extensions/migrate-hermes/model.apply.test.ts (3 testes)
```

**117/117 passaram, 0 falhas.**

### 3. Build real da imagem

```
docker build -t openclaw:local --build-arg OPENCLAW_INSTALL_DOCKER_CLI=1 .
```

executado no diretório `openclaw/` (contexto do `Dockerfile`). Nova imagem:
`sha256:8ac38920b23250b94a63b83d1679c7c1c2f0f80a5c5a87d0d24b10802042233d`.

### 4. Confirmação do conteúdo da imagem nova

```
docker run --rm --entrypoint /bin/sh openclaw:local -c \
  "grep -rhoE 'busy_timeout[^0-9]{0,10}[0-9]+' /app/dist/*.js" | sort -u
```

Resultado: `busy_timeout = 0`, `= 1000`, `= 2000`, `= 3000` — os valores
esperados de cada call site (`manager-reindex-lock.ts`=0,
`qmd-manager.ts`=1000, `repairOpenClawStateDatabaseSchema`=2000, resto=3000).
**Nenhuma ocorrência de `30000` restante em lugar nenhum do bundle.**

### 5. Redeploy real (down/up, não restart)

```
docker compose down    # removeu gateway + cli + rede
docker compose up -d   # recriou com a imagem nova
```

Confirmado via `docker inspect openclaw-openclaw-gateway-1 --format '{{.Image}}'`
→ `sha256:8ac38920b232...`, batendo com a imagem recém-buildada. `docker
compose ps` mostrou ambos os containers (`gateway`, `cli`) "healthy" segundos
depois de subir. O mesmo grep repetido **dentro do container rodando**
(não só na imagem parada) confirmou de novo só `0/1000/2000/3000`.

### 6. Teste de regressão "oi"

`healthz` respondendo OK (`{"ok":true,"status":"live"}`, ~1-3ms) durante
todo o processo, sem degradação. Usuário mandou "oi" real pelo WhatsApp;
logs do gateway mostraram 2 tentativas de "WhatsApp Cloud reply started"
(09:25:10 e 09:25:16 UTC) e nada de anormal depois — `healthz` continuou
rápido o tempo todo (event loop nunca travou). Usuário confirmou
recebimento da resposta normal, sem demora. Fluxo comum do WhatsApp segue
funcional depois do redeploy.

### 7. Reprodução ao vivo re-confirmada contra o código de produção real

Rodar vitest literalmente dentro do container de produção não é viável: a
imagem não empacota os arquivos de teste nem a árvore fonte completa (só
`dist/` compilado + um `src/agents` parcial de templates, sem
`bug4-sqlite-compound-repro.test.ts`). Alternativa acordada com o usuário
(evitando travar o SQLite real de produção com tráfego ao vivo): rodar o
teste no host contra o commit exato que gerou a imagem rodando
(`0e70b5f371`, mesmo HEAD do `docker build`) — código idêntico, banco
sempre isolado (o teste já usa `OPENCLAW_STATE_DIR` num diretório temporário
próprio, nunca `~/.openclaw` real).

Resultado:

```json
{
  "phase": 2,
  "enqueueBlockMs": 3010,
  "sendOutcome": "threw",
  "sendError": "code=ERR_SQLITE_ERROR causeCode=undefined message=database is locked",
  "platformSendCalls": 0,
  "auditBlockMs": 3009,
  "auditOutcome": "threw",
  "auditError": "code=PLUGIN_STATE_WRITE_FAILED causeCode=ERR_SQLITE_ERROR message=Failed to register plugin state entry.",
  "totalStackedMs": 6019
}
```

Contra os `60167ms` (`30080+30087`) medidos na sessão anterior com o
`busy_timeout` de 30s. Mecanismo muda exatamente como esperado: bloqueio
curto, erro capturável rápido, envio de plataforma continua não disparando
(comportamento write-ahead preservado). A asserção antiga do próprio teste
(`toBeGreaterThan(25_000)`) não foi atualizada — é esperado, esse arquivo é
throwaway/investigativo (não fazia parte do escopo pedido dos 2 testes a
corrigir) e segue marcado para deleção quando o Bug 4 fechar de vez.

### 8. Instrumentação `--report-on-signal`

Não repetida nesta sessão, por decisão explícita do usuário: já demonstrado
na auditoria que esse mecanismo não consegue capturar o estado durante um
travamento síncrono real (só depois que ele termina). O teste de reprodução
controlada já é evidência suficiente do mecanismo em si.

## Estado final

- **Produção:** rodando a imagem `sha256:8ac38920b232...`, buildada a partir
  do commit `0e70b5f371` (openclaw/), que inclui o fix `42af0a959a` +
  correção dos 2 testes de pragma. Confirmado por grep direto no bundle
  rodando: zero `30000` restante. `healthz` OK, WhatsApp real testado e
  respondendo normal.
- **`github-repo-report`:** continua desconectado (`plugins.entries` só com
  `deepseek`). **Não foi reconectado** — decisão fica para depois, por
  pedido explícito do usuário.
- **Testes:** 117/117 passando nas áreas tocadas pelo fix. Teste throwaway
  `bug4-sqlite-compound-repro.test.ts` com asserção antiga não atualizada
  (fora do escopo desta correção, documentado como dívida conhecida).
- **Git (`openclaw/`):** HEAD ainda destacado (`detached from v2026.6.9`),
  não ancorado em nenhuma branch — mesmo risco já registrado ontem
  (commit locais, não vão pro GitHub, e agora nem estão numa branch local).
  Não resolvido nesta sessão; segue como pendência.
- **Causa raiz do incidente original de 15/07:** o mecanismo (SQLite síncrono
  + `busy_timeout`) está confirmado por reprodução controlada e agora
  redeployado de verdade em produção. A ligação causal específica com aquele
  incidente exato segue circunstancial — a única evidência ao vivo desse
  incidente (`eventLoopDelayMaxMs=30064.8`) aponta o trabalho ativo como
  `model_call:stream_progress`, não uma escrita SQLite explícita. Ainda não
  identificado quem segurava o lock às 22:23 de 15/07.

## Lição desta sessão

A causa da falha de deploy da tentativa anterior foi confiar que
`docker compose restart` (ou `up` sem rebuild prévio) aplicaria um commit
novo — não aplica, ele só reinicia o processo com a imagem já existente.
Toda mudança de código que precisa chegar em produção via Docker exige
confirmar a imagem em uso (`docker inspect --format '{{.Image}}'` e
comparar com a saída de `docker build`), não só assumir que o restart
"pegou" a mudança. Ver `[[feedback_verify_dependency_source_before_fix]]`
e `[[feedback_revert_temp_config_immediately]]` — mesmo padrão de "produção
divergindo do que se acredita estar rodando" já visto antes.
