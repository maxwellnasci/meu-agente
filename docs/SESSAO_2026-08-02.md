# Sessão 2026-08-02

## Migração Amigão Kali → Contabo: Etapas 6 e 7 (cutover do túnel)

Retomada da migração de onde a sessão de 2026-07-29 tinha parado (Etapas
0-5 concluídas e validadas: backup, rsync do estado, transferência da
imagem Docker, compose ajustado, gateway subindo `healthy` no Contabo,
credencial WhatsApp válida direto do Contabo). Ver
[SESSAO_2026-08-02.md#etapa-6](#etapa-6--cloudflared-instalado-no-contabo-2-conectores-simultâneos)
abaixo para o que faltava.

### Checkpoint inicial — confirmação de estado antes de mexer em nada

Antes de qualquer ação, confirmado:
- **Kali:** gateway `healthy`, `cloudflared.service` ativo (~1h de
  uptime), túnel operacional.
- **Contabo:** gateway `healthy` há 4 dias, sem conflito de porta com
  os outros 11 containers já rodando lá (n8n, chatwoot, evolution,
  nginx-proxy-manager, mxos-login).

### Etapa 6 — `cloudflared` instalado no Contabo, 2 conectores simultâneos

1. **Versão:** `cloudflared` 2026.7.1 (mesma versão do Kali) instalado
   no Contabo via `.deb` baixado direto do GitHub releases
   (`cloudflare/cloudflared`), pra manter paridade exata.
2. **Credenciais copiadas:** `credentials-file`
   (`658015b4-c546-46a8-9c86-bf8986752688.json`) e `cert.pem`, via
   `scp` de `~/.cloudflared/` (Kali) pra `/etc/cloudflared/` (Contabo,
   dono root, `chmod 600`).
3. **`config.yml`** replicado igual ao do Kali, só ajustando o path do
   `credentials-file` pra `/etc/cloudflared/...json` (Contabo roda
   direto como root, sem precisar do path de usuário `/home/max/...`
   do Kali).
4. **Systemd unit** idêntico ao do Kali (`Type=notify`,
   `Restart=on-failure`), `daemon-reload` + `enable` + `start`.
5. **Confirmado via `cloudflared tunnel info 658015b4-...`:** 2
   `CONNECTOR ID` distintos ativos ao mesmo tempo — um do Kali
   (origin `186.214.53.181`) e um do Contabo (origin
   `158.220.125.233`), ambos `linux_amd64 2026.7.1`. Os dois gateways
   Docker seguiam `healthy` nos dois hosts.

**Risco identificado no checkpoint:** com 2 conectores do mesmo túnel
registrados na Cloudflare, a borda passa a balancear requisições entre
os dois origins — ou seja, mensagens do WhatsApp já podiam estar sendo
roteadas ora pro Kali ora pro Contabo desde esse momento, não só depois
do cutover formal. O estado (`~/.openclaw/`) só tinha sido sincronizado
uma vez, na Etapa 1 (2026-07-29) — qualquer sessão/conversa nova desde
então não existia no Contabo. Por isso o plano original já prescrevia
manter essa janela "curta e controlada", e a Etapa 7 foi decidida para
rodar em seguida, sem demora.

### Etapa 7 — Cutover: Kali desligado, Contabo assume sozinho

Executado a pedido explícito do usuário ("fechar a janela de 2
conectores é prioridade"), em 4 passos:

1. **`systemctl stop cloudflared` no Kali** — rodado pelo usuário
   (sudo interativo não pôde ser automatizado pela sessão do agente).
   Confirmado via `systemctl status`: `inactive (dead)`,
   `code=exited, status=0/SUCCESS`.
2. **Confirmação do corte:** `cloudflared tunnel info` voltou a
   mostrar só **1 CONNECTOR ID** — `00ea6b92-9432-4be8-b656-68ab38e26f97`,
   origin `158.220.125.233` (Contabo). O connector do Kali
   (`89f60875...`) desapareceu da lista.
3. **Teste real de entrega:** usuário mandou mensagem real pelo
   WhatsApp. Um `Monitor` com `docker logs -f` filtrado
   (`grep -iE 'whatsapp-cloud|inbound|error|fatal|panic'`) no gateway
   do Contabo capturou dois eventos `WhatsApp Cloud reply started for
   554184445755`, às **22:56:15 e 22:56:22 UTC**, com o trace do
   agente (`agent/embedded`) completando a sessão inteira sem erro
   (`sessionId=8ac04ebc-9749-4bde-b4f1-b68f64f3c24a`).
4. **Verificação de pendências:** zero `error`/`fatal`/`panic`/
   `timeout` no Contabo nas 2h anteriores ao teste; Kali não recebeu
   nenhuma mensagem nova depois do cutover (última resposta registrada
   no Kali foi às 22:29:33 UTC, **antes** do stop às ~22:51 UTC);
   `cloudflared` do Contabo seguia estável (sem restart loop) 12min
   depois do start.

**Duração real da janela de 2 conectores: ~8 minutos** (22:43–22:51
UTC) — dentro do previsto, sem qualquer sintoma de divergência de
estado entre os dois hosts durante essa janela curta.

### Resultado final da sessão

**O Amigão está rodando 100% em produção no servidor Contabo.** O Kali
não processa mais nenhuma mensagem de WhatsApp — mantém só o container
antigo (`openclaw-openclaw-gateway-1`) parado/idle como fallback, e o
`cloudflared` local desligado.

**Decisão sobre a Etapa 8 (parar de vez o container antigo do Kali):**
24h de observação, como o plano original previa. Agendado para
**2026-08-03, por volta de 22:56 UTC** (mesmo horário do cutover de
hoje), **condicional** a tudo continuar estável até lá. Detalhes e
critérios de estabilidade em
[PROXIMOS_PASSOS.md](PROXIMOS_PASSOS.md#migração-contabo).

## Processo zumbi encontrado e resolvido no Contabo

Ainda na mesma sessão, já dentro da janela de observação de 24h, veio
um relatório externo de um processo travado no Contabo: PID 215
(dentro do container), comando `cat`, ~99.8% de CPU, rodando há mais
de 4 dias. Investigado com evidência real antes de qualquer ação, sem
matar nada às cegas.

**1. O processo existe de verdade?** Confirmado no host via `ps`: PID
**813809**, `ELAPSED 4-13:01:22` (4 dias e 13h), `%CPU 99.8`, estado
`R` (rodando ativamente, não bloqueado). `sudo grep NSpid
/proc/813809/status` retornou `NSpid: 813809  215` — confirma que é
exatamente o PID 215 relatado (215 é a numeração vista de dentro do
namespace do container).

**2. O que tinha em `/tmp/check-token.js`?** O arquivo **não existia**
(`ls` dentro do container: `No such file or directory`). A explicação
apareceu ao inspecionar os file descriptors do processo
(`sudo ls -la /proc/813809/fd/`): `stdin` (fd 0) e `stdout` (fd 1)
apontavam pro **mesmo pipe** (`pipe:[23650967]`). O `cat` estava lendo
e reescrevendo nele mesmo, num loop infinito — nunca alcançou EOF,
nunca escreveu o arquivo de verdade. É por isso que o `%CPU` ficava
travado em ~100%: não era um processo bloqueado esperando (que usaria
~0% CPU), era um loop realmente girando.

**3. Tem relação com o Amigão/OpenClaw?** Nenhuma funcional. Árvore de
processos completa (`ps -eo pid,ppid,cmd`) mostrou a cadeia
`docker exec -i (813781, já órfão, PPID 1) → sh -c "cat > ..." (813803)
→ cat (813809)`, totalmente separada do processo real do gateway
(`812060`, `node dist/index.js gateway`). Ambos só compartilham o
mesmo container via `docker exec`, sem relação de pai/filho.

**4. Seguro matar?** Sim — confirmado via `docker inspect` que o PID
principal do container (`.State.Pid`) é `811962`, diferente da árvore
do `cat` preso, e que o `containerd-shim` (`811936`, único por
container, multiplexa sessões de exec) continuaria vivo e intacto
normalmente após encerrar só essa sessão de exec específica.

**Correlação com o histórico:** o processo estava rodando desde
**29/07**, o mesmo dia da Etapa 5 da migração (validação do token
WhatsApp via `/debug_token`) — bate com um resquício daquele teste,
provavelmente uma sessão SSH que caiu no meio de um
`docker exec -i ... sh -c "cat > arquivo"`.

**Ação:** `kill -9` nos 3 PIDs da cadeia (`813781`, `813803`, `813809`).
Confirmado depois: container `running`/`healthy`, processo real do
gateway (`812060`) com o mesmo `ELAPSED` de antes (sem restart), `top`
mostrando `load average` normalizado (~1.7–2.0 num host de 4 vCPU),
sem nenhum processo em CPU anormal.

**Lição aprendida e checklist reutilizável:** documentado em
[MANUTENCAO.md](MANUTENCAO.md#processos-zumbi-de-sessões-docker-exec--i-órfãs-encontrado-2026-08-02),
incluindo os comandos de diagnóstico usados aqui, prontos pra reusar
numa próxima vez.

**Isso não afeta a janela de observação de 24h da migração** — é um
achado independente, não ligado ao cutover em si; o gateway seguiu
saudável o tempo todo, sem interrupção.

---

Nenhuma outra ação ativa foi tomada no sistema depois disso — sessão
fechada só com documentação e commit.
